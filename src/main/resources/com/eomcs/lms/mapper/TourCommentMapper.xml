<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.eomcs.lms.dao.TourCommentDao">
  
  <resultMap type="tourComment" id="commentMap">
    <id column="tour_comment_id" property="no"/>
    <result column="member_id" property="memberNo"/>
    <result column="tour_id" property="tourNo"/>
    <result column="ranking" property="order"/>
    <result column="level" property="level"/>
    <result column="content" property="content"/>
    <result column="created_date" property="createdDate"/>
  </resultMap>
  
  <sql id="select1">
    select 
      tour_comment_id,
      member_id,
      tour_id, 
      ranking, 
      level, 
      content,
      created_date 
    from 
      tour_comment
  </sql>
  
  <select id="findAll" resultMap="commentMap">
    <include refid="select1"/>
    order by 
      tour_comment_id asc
  </select>
  
 <!--  <select id="findByNo" resultMap="boardMap" parameterType="int">
    <include refid="select1"/>
    where 
      board_id = #{value}
  </select>
  
  <update id="increaseCount" parameterType="int">
    update lms_board 
    set 
      vw_cnt = vw_cnt + 1 
    where board_id = #{value}
  </update>
  -->
  
  <insert id="insert" parameterType="tourComment">
    insert into tour_comment(member_id, tour_id, ranking, level, content) 
    values(#{memberNo},#{tourNo},#{order},#{level},#{content})
  </insert>
   
   <!--
  <update id="update" parameterType="board">
    update lms_board 
    set 
      conts = #{contents} 
    where board_id = #{no}
  </update>
  
  <delete id="delete" parameterType="int">
    delete from lms_board 
    where board_id = #{no}
  </delete>
  
  <select id="countAll" resultType="int">
    select count(*) from lms_board
  </select> -->
</mapper>










